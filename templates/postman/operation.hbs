<!--
Available variables:
- anchor: the HTML anchor for this operation
- method: HTTP method (GET, POST, etc.)
- path: the API path (e.g., /users/{id})
- operation
   - summary: brief description of the operation
   - description: detailed description of the operation (in HTML)
   - parametersHeader: pre-rendered HTML for the parameters header
   - parameters: pre-rendered HTML for the parameters block
   - responses: pre-rendered HTML for the responses block
-->

<div class="operation" id="{{anchor}}">
    <div class="operation-header">
        <div class="operation-method {{method}}">{{method}}</div>
        <div class="operation-path">{{path}}</div>
    </div>
    {{#if operation.summary}}
        <div class="operation-summary">
            {{operation.summary}}
        </div>
    {{/if}}

    {{#if operation.description}}
        <div class="operation-description">
            <div>{{operation.description}}</div>
        </div>
    {{/if}}

    {{#if operation.parameters}}
        <h4>Parameters</h4>
        <div class="operation-parameters">
            <!-- this is a pre-rendered header for operation parameters -->
            {{{operation.parametersHeader}}}
            <!-- this is a pre-rendered block of operation parameters -->
            {{{operation.parameters}}}
        </div>
    {{/if}}

    {{#if operation.responses}}
        <h4>Responses</h4>
        <div class="operation-responses">
            <!-- this is a pre-rendered block of operation responses -->
            {{{operation.responses}}}
        </div>
    {{/if}}
</div>

